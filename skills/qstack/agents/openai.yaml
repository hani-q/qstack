interface:
  display_name: "QStack"
  short_description: "List installed QStack skills and recommend the next one"
  default_prompt: "Use $qstack to list every installed QStack skill and recommend which to run next."
policy:
  allow_implicit_invocation: false
