interface:
  display_name: "QStack Plan Adherence Review"
  short_description: "Score plan execution against code evidence"
  default_prompt: "Use $qstack-plan-adherence-review to compare the current plan with its execution record and code changes."
policy:
  allow_implicit_invocation: false
