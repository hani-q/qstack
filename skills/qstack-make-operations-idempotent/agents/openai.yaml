interface:
  display_name: "QStack Make Operations Idempotent"
  short_description: "Design retries that converge safely"
  default_prompt: "Use $qstack-make-operations-idempotent to make this operation converge across retries and partial failures."
policy:
  allow_implicit_invocation: true
