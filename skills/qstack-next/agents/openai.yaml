interface:
  display_name: "QStack Next"
  short_description: "Recommend the next skill from context and plan state"
  default_prompt: "Use $qstack-next to recommend which installed skill to run next."
policy:
  allow_implicit_invocation: true
