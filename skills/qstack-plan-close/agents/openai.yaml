interface:
  display_name: "QStack Plan Close"
  short_description: "Capture outcomes and promote durable lessons"
  default_prompt: "Use $qstack-plan-close to close out the shipped plan and preserve its durable lessons."
policy:
  allow_implicit_invocation: false
