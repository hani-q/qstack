interface:
  display_name: "QStack Model the Domain"
  short_description: "Shape domain state without forced abstractions"
  default_prompt: "Use $qstack-model-the-domain to replace these scattered assumptions with a fitting domain structure."
policy:
  allow_implicit_invocation: true
